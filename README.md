# نظام تقييم الموظفين - لوحة التحكم الإدارية

نظام شامل لإدارة وتقييم الموظفين مع واجهة عربية احترافية تدعم RTL ومحرر الاستمارات الديناميكي.

## المميزات الرئيسية

### 🎯 لوحة التحكم الإدارية
- واجهة عربية احترافية مع دعم RTL كامل
- تصميم متجاوب (Responsive) يعمل على جميع الأجهزة
- إحصائيات شاملة ولوحة معلومات متقدمة
- نظام أمان متقدم مع CSRF protection

### 🏢 إدارة الإدارات
- إنشاء وتعديل وحذف الإدارات
- تعيين المدراء للإدارات
- تفعيل/تعطيل الإدارات
- التحقق من البيانات المرتبطة قبل الحذف

### 📝 إدارة الاستمارات
- إنشاء وتعديل الاستمارات
- معاينة الاستمارات قبل النشر
- تفعيل/تعطيل الاستمارات
- إحصائيات الإجابات

### 🎨 محرر الاستمارات (Form Builder)
- واجهة drag-and-drop سهلة الاستخدام
- دعم جميع أنواع الحقول:
  - نص قصير وطويل
  - البريد الإلكتروني
  - الأرقام مع التحقق
  - التاريخ والوقت
  - القوائم المنسدلة
  - أزرار الاختيار
  - مربعات الاختيار
  - رفع الملفات
  - المجموعات المتكررة (Repeater)
- قواعد التحقق المتقدمة (min/max/regex)
- خيار "تحميل من الإدارات" للحقول الديناميكية
- إعادة ترتيب الحقول بالسحب والإفلات

### 🔒 الأمان والحماية
- CSRF protection على جميع النماذج
- التحقق من الصلاحيات
- Activity logging لتسجيل العمليات
- Sanitization للمدخلات

## هيكل المشروع

```
/
├── config/
│   └── database.php          # إعدادات قاعدة البيانات
├── src/
│   ├── Core/
│   │   ├── Services/
│   │   │   ├── DepartmentService.php
│   │   │   ├── FormService.php
│   │   │   └── FormFieldService.php
│   │   ├── Database.php
│   │   └── Logger.php
│   └── helpers.php           # الدوال المساعدة
├── public/
│   ├── admin/
│   │   ├── dashboard.php     # الصفحة الرئيسية
│   │   ├── departments.php   # إدارة الإدارات
│   │   ├── forms.php         # إدارة الاستمارات
│   │   ├── form-builder.php  # محرر الاستمارات
│   │   └── api/
│   │       ├── forms.php     # API الاستمارات
│   │       ├── departments.php # API الإدارات
│   │       └── form-fields.php # API الحقول
│   ├── preview-form.php      # معاينة الاستمارة
│   └── assets/
│       ├── css/admin.css     # أنماط CSS
│       └── js/admin.js       # JavaScript functions
├── storage/
│   └── forms/                # ملفات المرفوعة
├── database/
│   └── migrations/           # قاعدة البيانات
└── docs/                     # التوثيق
```

## متطلبات النظام

- PHP 8.0 أو أحدث
- MySQL 8.0 أو أحدث
- Apache أو Nginx
- امتدادات PHP المطلوبة:
  - PDO
  - PDO_MySQL
  - JSON
  - mbstring
  - OpenSSL

## التثبيت والإعداد

### 1. تحميل المشروع
```bash
git clone [repository-url]
cd employee-evaluation-system
```

### 2. تثبيت التبعيات
```bash
composer install
```

### 3. إعداد قاعدة البيانات
```bash
# إنشاء قاعدة البيانات
mysql -u root -p -e "CREATE DATABASE employee_evaluation;"

# استيراد الجداول
mysql -u root -p employee_evaluation < database/migrations/initial_schema.sql
```

### 4. إعدادات البيئة
إنشاء ملف `.env`:
```env
DB_HOST=localhost
DB_NAME=employee_evaluation
DB_USER=root
DB_PASS=your_password
```

### 5. تحديث إعدادات قاعدة البيانات
تحديث ملف `config/database.php` بالإعدادات المناسبة.

### 6. إعداد الصلاحيات
```bash
chmod 755 storage/forms/
chown -R www-data:www-data storage/forms/
```

## الاستخدام

### الوصول للوحة التحكم
1. افتح المتصفح واذهب إلى: `http://your-domain/public/admin/`
2. سيتم عرض لوحة التحكم الرئيسية

### إدارة الإدارات
1. انقر على "الإدارات" في القائمة الجانبية
2. لإدارة إدارة جديدة: انقر "إضافة إدارة جديدة"
3. لتعديل إدارة: انقر أيقونة التعديل بجانب الإدارة
4. لتفعيل/تعطيل: انقر أيقونة التشغيل/الإيقاف
5. للحذف: انقر أيقونة الحذف (مع التحذير من البيانات المرتبطة)

### إدارة الاستمارات
1. انقر على "الاستمارات" في القائمة الجانبية
2. لإنشاء استمارة جديدة: انقر "إنشاء استمارة جديدة"
3. لتعديل الاستمارة: انقر أيقونة التعديل
4. لمحرر الحقول: انقر أيقونة "محرر الحقول"
5. للمعاينة: انقر أيقونة "العين"

### استخدام محرر الاستمارات
1. من قائمة الاستمارات، انقر على أيقونة "محرر الحقول"
2. اختر نوع الحقل من الشبكة الجانبية
3. املأ بيانات الحقل في اللوحة الجانبية:
   - عنوان الحقل (مطلوب)
   - النص التوضيحي
   - النص المساعد
   - مطلوب/اختياري
   - قواعد التحقق
4. للحقول التي تحتاج خيارات (قائمة/اختيار):
   - اختر "خيارات ثابتة" وأدخل الخيارات سطر واحد لكل خيار
   - أو اختر "تحميل من الإدارات" لجلب قائمة الإدارات ديناميكياً
5. اضغط "حفظ الحقل" لإضافته للاستمارة
6. اسحب وأفلت الحقول لإعادة ترتيبها
7. اضغط "حفظ الترتيب" لحفظ التغييرات

### معاينة الاستمارة
1. من قائمة الاستمارات، انقر على أيقونة "العين" للمعاينة
2. أو من محرر الاستمارات، اضغط زر "معاينة"
3. ستفتح نافذة جديدة بمعاينة الاستمارة
4. املأ البيانات واختبر التحقق
5. أرسل الاستمارة لاختبار العملية الكاملة

## API Endpoints

### الاستمارات
```
GET    /api/forms              # قائمة جميع الاستمارات
GET    /api/forms?status=active # قائمة الاستمارات النشطة فقط
GET    /api/forms/{id}         # تفاصيل استمارة محددة
POST   /api/forms              # إنشاء استمارة جديدة
PUT    /api/forms/{id}         # تحديث استمارة
DELETE /api/forms/{id}         # حذف استمارة
POST   /api/forms/{id}/status  # تغيير حالة الاستمارة
```

### الحقول
```
GET    /api/form-fields/{form_id}                # قائمة حقول الاستمارة
GET    /api/form-fields/{form_id}/{field_id}     # تفاصيل حقل محدد
POST   /api/form-fields/{form_id}                # إضافة حقل جديد
PUT    /api/form-fields/{field_id}               # تحديث حقل
DELETE /api/form-fields/{field_id}               # حذف حقل
POST   /api/form-fields/{form_id}/reorder        # إعادة ترتيب الحقول
```

### الإدارات
```
GET    /api/departments                # قائمة جميع الإدارات
GET    /api/departments?is_active=true # قائمة الإدارات النشطة
GET    /api/departments/active         # الإدارات النشطة للاختيار
GET    /api/departments/managers       # قائمة المدراء
GET    /api/departments/{id}           # تفاصيل إدارة محددة
POST   /api/departments                # إنشاء إدارة جديدة
PUT    /api/departments/{id}           # تحديث إدارة
DELETE /api/departments/{id}           # حذف إدارة
POST   /api/departments/{id}/status    # تغيير حالة الإدارة
```

## الأمان

### CSRF Protection
- يتم إنشاء CSRF token لكل جلسة
- يتم التحقق من token في جميع النماذج والطلبات

### Input Validation
- التحقق من صحة البيانات على مستوى الخادم
- Sanitization للمدخلات لمنع XSS
- التحقق من أنواع البيانات المتوقعة

### Authorization
- التحقق من الصلاحيات قبل الوصول للصفحات
- REST API محمية من الوصول غير المصرح

### Activity Logging
- تسجيل جميع العمليات الحساسة
- تتبع التغييرات والتعديلات

## التطوير والتخصيص

### إضافة نوع حقل جديد
1. أضف الحقل في `$fieldTypes` في `form-builder.php`
2. أضف منطق العرض في `preview-form.php`
3. أضف قواعد التحقق المناسبة في `helpers.php`

### تخصيص الأنماط
- ملف CSS الرئيسي: `public/assets/css/admin.css`
- استخدم Bootstrap 5 RTL classes
- متغيرات CSS مخصصة للألوان والخطوط

### إضافة ميزات جديدة
- استخدم الخدمات الموجودة في `src/Core/Services/`
- اتبع نمط CRUD الموجود
- أضف API endpoints إذا لزم الأمر

## استكشاف الأخطاء

### مشاكل شائعة وحلولها

**خطأ في الاتصال بقاعدة البيانات:**
- تأكد من صحة إعدادات قاعدة البيانات في `config/database.php`
- تحقق من أن قاعدة البيانات موجودة وأن المستخدم له الصلاحيات

**خطأ في CSRF token:**
- تأكد من بدء الجلسة session_start()
- تحقق من أن session_id متطابق

**مشاكل في عرض النماذج:**
- تأكد من تحميل Bootstrap CSS/JS بشكل صحيح
- تحقق من وجود Font Awesome icons

**مشاكل في الرفع:**
- تحقق من صلاحيات مجلد `storage/forms/`
- تأكد من إعدادات upload_max_filesize في PHP

## الدعم والمساهمة

### الإبلاغ عن المشاكل
يرجى الإبلاغ عن أي مشاكل أو اقتراحات عبر النظام الداخلي للمتابعة.

### المساهمة في التطوير
- اتبع نمط الكود الموجود
- أضف تعليقات واضحة
- اختبر الميزات الجديدة
- وثق أي تغييرات في API

## الترخيص

هذا المشروع مرخص تحت رخصة [اختر الرخصة المناسبة].

## التحديثات المستقبلية

### الميزات المخططة
- [ ] تصدير البيانات (Excel, PDF)
- [ ] إشعارات البريد الإلكتروني
- [ ] تقارير وإحصائيات متقدمة
- [ ] إدارة المستخدمين والصلاحيات
- [ ] نظام المتابعة والتذكيرات
- [ ] تكامل مع أنظمة خارجية
- [ ] تطبيق للهواتف المحمولة

### التحديثات التقنية
- [ ] دعم PHP 8.2+
- [ ] تحسين الأداء والذاكرة
- [ ] إضافة اختبارات آلية
- [ ] تحسين أمان قاعدة البيانات
- [ ] إضافة نظام Cache

---

**ملاحظة:** هذا النظام مطور لتلبية احتياجات تقييم الموظفين في المؤسسات العربية، مع دعم كامل للغة العربية والـ RTL.